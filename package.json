{
  "name": "ahmad-bukhari-portfolio",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm run build && node scripts/serve-static.mjs",
    "build": "node scripts/build-seo-site.mjs",
    "start": "node scripts/serve-static.mjs",
    "test": "node --check scripts/serve-static.mjs && node --check static/decision-engine.js && node --check static/experience.js && node --check public/twin-widget.js && npm run build && node scripts/check-seo-build.mjs",
    "lint": "node scripts/check-seo-build.mjs"
  },
  "devDependencies": {
    "@fontsource-variable/instrument-sans": "^5.3.0",
    "@fontsource/ibm-plex-mono": "^5.3.0",
    "@fontsource/instrument-serif": "^5.3.0",
    "typescript": "^5.7.0"
  }
}
