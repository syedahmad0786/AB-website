{
  "name": "ahmad-bukhari-portfolio",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm run build && node scripts/serve-static.mjs",
    "build": "node scripts/build-seo-site.mjs",
    "start": "node scripts/serve-static.mjs",
    "test": "node --check scripts/serve-static.mjs && node --check public/twin-widget.js && npm run build && node scripts/check-seo-build.mjs",
    "lint": "node scripts/check-seo-build.mjs"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
